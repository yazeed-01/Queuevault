import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const background   = Color(0xFF050A18);
  static const surface      = Color(0xFF0D1123);
  static const surfaceLight = Color(0xFF141929);
  static const card         = Color(0xFF0D1123);
  static const cardBorder   = Color(0xFF1E2A3A);

  // Neon primaries
  static const primary      = Color(0xFF00FF41); // neon green
  static const primaryGlow  = Color(0x3300FF41);
  static const accent       = Color(0xFF00F0FF); // cyan
  static const accentGlow   = Color(0x3300F0FF);
  static const tertiary     = Color(0xFFFF0055); // hot pink
  static const tertiaryGlow = Color(0x33FF0055);

  // Text
  static const textPrimary   = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8892A4);
  static const textMuted     = Color(0xFF3D4A5C);

  // Semantic
  static const success     = Color(0xFF00FF41);
  static const successGlow = Color(0x3300FF41);
  static const warning     = Color(0xFFFFD600);
  static const error       = Color(0xFFFF0055);

  // Category colors (neon set)
  static const watchColor = Color(0xFF00F0FF); // cyan  — movies/series
  static const watchGlow  = Color(0x3300F0FF);
  static const animeColor = Color(0xFFFF0055); // pink  — anime
  static const animeGlow  = Color(0x33FF0055);
  static const playColor  = Color(0xFF00FF41); // green — games
  static const playGlow   = Color(0x3300FF41);
  static const buyColor   = Color(0xFFFFD600); // yellow — products
  static const buyGlow    = Color(0x33FFD600);

  static const divider          = Color(0xFF1E2A3A);
  static const shimmerBase      = Color(0xFF0D1123);
  static const shimmerHighlight = Color(0xFF1A2438);
}
